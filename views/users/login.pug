extends ../layout

block title
  = `Login`

block content
  h1.title Login
    form(action="/users/session", method="POST")
        .field
          .control 
            input.input(name="name")/
        .field
          .control
            button.button.is-link.is-fullwidth Login